In this directory copy the following files:

> Copy the appropriate AppDriver.yaml file from sample config/app-drivers to driver-config/ directory
> Android / iOS app file e.g. .app, .api, .apk etc. and specify its path in AppDriver.yaml file.
> (if applicable) firefox profiles into profiles/ directory
 